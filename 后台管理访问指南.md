# 后台管理访问指南

## 📍 访问地址

启动前端后，直接在浏览器访问：

```
http://localhost:5173/admin/users
```

## 🔐 权限要求

- 需要 **管理员（admin）** 或 **超级管理员（superadmin）** 权限
- 普通用户（user）无法访问，会被重定向到首页

## 🚀 创建第一个管理员账户

新注册的用户默认是 `user` 角色，需要先创建一个管理员账户。有两种方式：

### 方式 A：手动修改数据库（推荐）

1. **找到数据库文件**
   - 路径：`Sever/database/paper_analysis.db`

2. **使用 SQLite 工具打开数据库**
   - 可以使用 SQLite Browser、DB Browser for SQLite 或命令行工具

3. **执行 SQL 更新**
   ```sql
   -- 将指定用户提升为管理员（替换 username 为你的用户名）
   UPDATE auth_users 
   SET role = 'admin', updated_at = datetime('now') 
   WHERE username = '你的用户名';
   
   -- 或者提升为超级管理员
   UPDATE auth_users 
   SET role = 'superadmin', updated_at = datetime('now') 
   WHERE username = '你的用户名';
   ```

4. **验证**
   - 重新登录后，访问 `http://localhost:5173/admin/users` 即可进入后台

### 方式 B：使用 Python 脚本

创建一个临时脚本 `create_admin.py`：

```python
import sqlite3
import os

DB_PATH = os.path.join("Sever", "database", "paper_analysis.db")

conn = sqlite3.connect(DB_PATH)
cursor = conn.cursor()

# 替换为你的用户名
username = "你的用户名"

# 更新为管理员
cursor.execute(
    "UPDATE auth_users SET role = 'admin', updated_at = datetime('now') WHERE username = ?",
    (username,)
)

conn.commit()
conn.close()

print(f"用户 {username} 已提升为管理员")
```

运行：
```bash
python create_admin.py
```

## 📋 后台管理功能

进入后台后，可以管理：

### 用户管理
- **用户等级**：管理用户的访问等级（free/pro/pro_plus）
- **权限管理**（仅 superadmin）：管理用户角色（user/admin/superadmin）

### 运维管理
- **脚本执行**：手动运行 Pipeline
- **定时调度**：配置自动定时执行

### 系统配置
- **系统配置**：管理系统配置项
- **模型配置**：管理大模型配置
- **提示词配置**：管理提示词配置

## ⚠️ 注意事项

1. 确保已登录账户
2. 账户必须具有 admin 或 superadmin 权限
3. 如果访问被拒绝，检查账户角色是否正确
