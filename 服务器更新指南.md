# 服务器更新指南

本文档说明如何在代码更新后更新服务器上的部署。

## 📋 前置说明

- **项目路径**：`/projects/ArxivPaper4`
- **后端端口**：8000（systemd 服务：`arxiv-api`）
- **前端端口**：5173（开发模式）或 80（Nginx 生产模式）

---

## 🚀 完整更新流程

### 第一步：连接到服务器

```bash
ssh root@你的服务器IP
# 或使用你配置的 SSH 密钥
```

### 第二步：进入项目目录

```bash
cd /projects/ArxivPaper4
```

### 第三步：拉取最新代码

如果你使用 Git 管理代码：

```bash
# 拉取最新代码
git pull origin main
# 或 git pull origin master（根据你的主分支名称）

# 查看更新内容（可选）
git log --oneline -10
```

如果你使用其他方式部署代码（如 FTP、rsync 等），请先完成代码同步。

---

## 🔧 后端更新

### 1. 检查是否有新的依赖

```bash
cd /projects/ArxivPaper4/Sever

# 检查 requirements.txt 是否有更新
# 如果有新依赖，安装它们
pip install -r requirements.txt
```

### 2. 重启后端服务

```bash
# 重启 systemd 服务
systemctl restart arxiv-api

# 检查服务状态
systemctl status arxiv-api --no-pager
```

### 3. 查看后端日志（如有问题）

```bash
# 查看最近 50 行日志
journalctl -u arxiv-api -n 50 --no-pager

# 实时查看日志（按 Ctrl+C 退出）
journalctl -u arxiv-api -f
```

---

## 🎨 前端更新

根据你的部署方式选择对应的更新方法：

### 方式一：开发模式（端口 5173）

如果你使用 `npm run dev` 开发模式运行前端：

#### 如果使用 tmux 运行：

```bash
# 进入 tmux 会话
tmux attach -t arxiv

# 切换到前端窗格（使用 Ctrl+b 然后方向键）
# 停止当前进程（Ctrl+C）
# 重新启动
cd /projects/ArxivPaper4/View
npm install  # 如果有新的依赖
npm run dev -- --host 0.0.0.0 --port 5173

# 离开 tmux：Ctrl+b 然后按 d
```

#### 如果使用 systemd 服务运行前端：

```bash
# 检查是否有前端服务（假设服务名为 arxiv-view）
systemctl list-units | grep arxiv

# 如果有前端服务，重启它
systemctl restart arxiv-view

# 检查状态
systemctl status arxiv-view --no-pager
```

#### 如果使用 PM2 或其他进程管理器：

```bash
# PM2 示例
cd /projects/ArxivPaper4/View
npm install  # 如果有新的依赖
pm2 restart arxiv-view
# 或
pm2 reload arxiv-view
```

### 方式二：生产模式（Nginx 托管，端口 80）

如果你使用 Nginx 托管构建后的静态文件：

```bash
# 1. 进入前端目录
cd /projects/ArxivPaper4/View

# 2. 安装新依赖（如果有）
npm install

# 3. 确保构建工具可执行
chmod +x node_modules/.bin/vite
chmod +x node_modules/.bin/tsc
chmod +x node_modules/typescript/bin/tsc

# 4. 重新构建前端
npm run build

# 5. 检查构建结果
ls -la dist

# 6. 重载 Nginx（无需重启，零停机）
systemctl reload nginx
```

#### Nginx 配置说明

确保你的 Nginx 配置包含以下反向代理规则（完整配置请参考 `cd.md`）：

```nginx
# 移动端静态资源（Mobile build 产物）
location /m/assets/ {
    alias /projects/ArxivPaper4/Mobile/dist/assets/;
}

# 移动端 SPA 路由
location /m/ {
    alias /projects/ArxivPaper4/Mobile/dist/;
    try_files $uri $uri/ /m/index.html;
}

location = /m/index.html {
    alias /projects/ArxivPaper4/Mobile/dist/index.html;
}

# /api/xxx -> http://127.0.0.1:8000/xxx
location /api/ {
    proxy_pass http://127.0.0.1:8000/;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}

# /static/xxx -> http://127.0.0.1:8000/static/xxx (PDF.js viewer, kb_files, data)
location /static/ {
    proxy_pass http://127.0.0.1:8000/static/;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}
```

**重要**：`/static/` 代理是必需的，用于：
- PDF.js 阅读器（`/static/pdfjs/web/viewer.html`）
- 知识库文件（`/static/kb_files/...`）
- 数据文件（`/static/data/...`）

另外，如果你希望手机自动进入移动端，Nginx 必须有 `/m/` 与 `/m/assets/` 规则，并且 `Mobile` 端要用 `/m/` 作为构建 base。

如果缺少此配置，PDF 阅读器将无法正常工作。

---

## ✅ 验证更新是否成功

### 1. 检查后端服务状态

```bash
# 检查服务是否正常运行
systemctl status arxiv-api --no-pager

# 测试后端 API
curl -I http://127.0.0.1:8000/docs
# 或
curl http://127.0.0.1:8000/api/health  # 如果有健康检查接口
```

### 2. 检查前端服务状态

**开发模式（5173端口）：**
```bash
# 检查端口是否在监听
netstat -tlnp | grep 5173
# 或
ss -tlnp | grep 5173

# 测试前端
curl -I http://127.0.0.1:5173
```

**生产模式（Nginx）：**
```bash
# 检查 Nginx 状态
systemctl status nginx --no-pager

# 测试前端
curl -I http://127.0.0.1
curl -I http://127.0.0.1/m/
curl -I http://127.0.0.1/m/assets/
```

### 3. 浏览器验证

在浏览器中访问：
- **开发模式**：`http://你的服务器IP:5173`
- **生产模式**：`http://你的服务器IP` 或 `http://你的域名`

检查：
- ✅ 页面能正常加载
- ✅ 功能是否正常（登录、数据展示等）
- ✅ 浏览器控制台没有报错（F12 打开开发者工具）
- ✅ 手机访问根路径会进入 `/m/...` 移动端页面
- ✅ iPad/平板和桌面访问保持默认桌面版页面

---

## 🔍 常见问题排查

### 后端启动失败

```bash
# 查看详细错误日志
journalctl -u arxiv-api -n 200 --no-pager

# 常见原因：
# 1. Python 依赖缺失 -> pip install -r requirements.txt
# 2. 端口被占用 -> netstat -tlnp | grep 8000
# 3. 配置文件错误 -> 检查 Sever/config/config.py
```

### 前端构建失败

```bash
# 查看构建错误信息
cd /projects/ArxivPaper4/View
npm run build

# 常见原因：
# 1. Node 版本不兼容 -> node --version
# 2. 依赖缺失 -> npm install
# 3. TypeScript 错误 -> 检查代码语法
```

### 前端页面空白或 404

```bash
# 检查 Nginx 配置
nginx -t

# 检查 dist 目录是否存在
ls -la /projects/ArxivPaper4/View/dist

# 检查 Nginx 错误日志
tail -f /var/log/nginx/error.log
```

### 服务无法访问

```bash
# 检查防火墙
firewall-cmd --list-ports
# 或
iptables -L -n

# 检查服务是否在监听
netstat -tlnp | grep -E '5173|8000|80'
```

---

## 📝 快速更新脚本（可选）

你可以创建一个更新脚本简化操作：

```bash
# 创建更新脚本
vi /projects/ArxivPaper4/update.sh
```

脚本内容：

```bash
#!/bin/bash

set -e  # 遇到错误立即退出

echo "开始更新 ArxivPaper4..."

# 进入项目目录
cd /projects/ArxivPaper4

# 拉取代码（如果使用 Git）
if [ -d .git ]; then
    echo "拉取最新代码..."
    git pull
fi

# 更新后端
echo "更新后端..."
cd Sever
pip install -r requirements.txt -q
systemctl restart arxiv-api
echo "后端已重启"

# 更新前端（根据你的部署方式选择）
echo "更新前端..."

# 方式一：开发模式
# cd ../View
# npm install
# # 如果使用 systemd，重启服务
# # systemctl restart arxiv-view

# 方式二：生产模式（Nginx）
cd ../View
npm install
chmod +x node_modules/.bin/vite
chmod +x node_modules/.bin/tsc 2>/dev/null || true
npm run build
systemctl reload nginx
echo "前端已更新"

# 检查服务状态
echo ""
echo "检查服务状态..."
systemctl status arxiv-api --no-pager | head -5
systemctl status nginx --no-pager | head -5

echo ""
echo "✅ 更新完成！"
```

使用脚本：

```bash
# 添加执行权限
chmod +x /projects/ArxivPaper4/update.sh

# 运行更新
/projects/ArxivPaper4/update.sh
```

---

## 🎯 更新检查清单

每次更新后，请确认：

- [ ] 代码已成功拉取/同步
- [ ] 后端依赖已更新（如有新依赖）
- [ ] 后端服务已重启且状态正常
- [ ] 前端依赖已更新（如有新依赖）
- [ ] 前端已重新构建/重启
- [ ] 浏览器访问正常
- [ ] 核心功能测试通过
- [ ] 无错误日志

---

## 📞 需要帮助？

如果更新过程中遇到问题：

1. **查看服务日志**：
   ```bash
   journalctl -u arxiv-api -n 100 --no-pager
   ```

2. **检查服务状态**：
   ```bash
   systemctl status arxiv-api --no-pager
   systemctl status nginx --no-pager
   ```

3. **回滚到上一个版本**（如果使用 Git）：
   ```bash
   cd /projects/ArxivPaper4
   git log --oneline -10  # 查看提交历史
   git checkout <上一个提交的hash>
   # 然后重新执行更新流程
   ```

---

**最后更新**：请根据你的实际部署方式调整上述步骤。
