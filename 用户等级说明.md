# 用户等级说明（未登录 / 普通 / Pro / Pro+）

本文基于当前代码实现梳理“未登录用户、普通用户、Pro、Pro+”的实际差异。

## 一句话结论

- 当前版本里，**普通用户（free）和未登录用户在论文浏览配额上是一样的：都为 3 条/天**。
- `Pro` 配额为 **15 条/天**。
- `Pro+` 配额为 **无限制**。
- 是否登录，决定了能否使用“个人中心、知识库、笔记、对比分析、个人预设”等登录态功能。

## 等级对照表

| 用户状态 | 后端标识 | 日报/论文列表配额（按单日） | 登录态功能 | 典型提示文案 |
| --- | --- | --- | --- | --- |
| 未登录用户 | `anonymous`（会话不存在） | 3 条 | 不可用 | “未登录账号上限（3条）” |
| 普通用户 | `free` | 3 条 | 可用 | “普通账号上限（3条）” |
| Pro | `pro` | 15 条 | 可用 | “Pro账号上限（15条）” |
| Pro+ | `pro_plus` | 无限制 | 可用 | 无上限提示 |

## 当前规则细节

### 1) 配额规则（后端实际逻辑）

- 配额函数在后端统一计算：未登录返回 `3`，`free` 返回 `3`，`pro` 返回 `15`，`pro_plus` 返回 `None`（无限制）。
- 配额会同时应用在：
  - `GET /api/papers`（论文列表）
  - `GET /api/digest/{date}`（日报）
- 返回值中会带上：
  - `quota_limit`：当前可见上限（`null` 代表无限制）
  - `tier`：当前等级（未登录时为 `anonymous`）

### 2) “未登录”和“普通用户”到底差在哪

虽然两者配额同为 3 条，但**是否登录**会影响功能可用性：

- 未登录：只能使用公开浏览能力（如日报/论文详情等公开接口）。
- 普通用户（已登录）：可使用需要登录的能力，包括但不限于：
  - 个人中心（`/profile`）
  - 知识库（收藏论文、文件夹、笔记、附件、批注）
  - 对比分析与对比结果保存
  - 用户级设置与用户预设（LLM 预设、Prompt 预设）

### 3) Pro 与 Pro+

- `Pro`：在普通用户能力基础上，把浏览配额提升到 15 条。
- `Pro+`：在普通用户能力基础上，浏览配额无限制。

### 4) 管理员角色的特殊说明（避免混淆）

系统里“角色（role）”和“等级（tier）”是两个维度：

- 角色：`user / admin / superadmin`
- 等级：`free / pro / pro_plus`

当前实现中，`admin` 和 `superadmin` 会被按“不受配额限制”处理，并在返回给前端时展示为 `pro_plus` 级别效果（即无限制）。

## 给产品/运营对外说明的建议口径

可直接使用以下简版：

- 未登录：可试用，最多查看 3 条；
- 普通用户：登录后可用个人功能，浏览上限仍为 3 条；
- Pro：浏览上限提升到 15 条；
- Pro+：浏览无限制。

---

如后续要调整策略（例如把普通用户从 3 提升到 10），只需修改后端配额函数即可，前端会随接口返回自动更新展示。
